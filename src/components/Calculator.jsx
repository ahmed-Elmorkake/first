import { useMemo, useState } from 'react'
import AccessGate from './AccessGate'
const money=value=>new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:0}).format(value)
function Calculator({ onAuthOpen }){const [price,setPrice]=useState(1250000),[down,setDown]=useState(20),[years,setYears]=useState(25),[rate,setRate]=useState(4.5);const result=useMemo(()=>{const principal=price*(1-down/100),n=years*12,r=rate/100/12;const monthly=r?principal*(r*(1+r)**n)/((1+r)**n-1):principal/n;return{monthly,total:monthly*n,principal}},[price,down,years,rate]);const field=(label,value,setter,min,max,step,prefix='')=><label className="block"><span className="flex justify-between text-sm font-bold"><span>{label}</span><span className="text-amber-700">{prefix}{value}{label==='Down payment'||label==='Interest rate'?'%':''}</span></span><input className="mt-3 w-full accent-amber-600" type="range" min={min} max={max} step={step} value={value} onChange={e=>setter(Number(e.target.value))}/></label>;return <section id="calculator" className="bg-slate-900 py-24 text-white sm:py-32"><div className="mx-auto grid max-w-7xl gap-12 px-6 lg:grid-cols-2 lg:px-8"><div><p className="text-xs font-bold tracking-[.2em] text-amber-300">OWN WITH CONFIDENCE</p><h2 className="mt-3 font-serif text-5xl sm:text-6xl">Estimate your <em className="font-normal">investment.</em></h2><p className="mt-6 max-w-md text-white/65">Use our mortgage calculator to understand your potential monthly payment.</p></div><AccessGate onSignIn={onAuthOpen}><div className="rounded-2xl bg-white p-6 text-slate-900 sm:p-8"><div className="space-y-7">{field('Property price',price,setPrice,500000,5000000,50000,'$')}{field('Down payment',down,setDown,5,60,1)}{field('Loan duration',years,setYears,5,35,1)}{field('Interest rate',rate,setRate,1,10,.1)}</div><div className="mt-8 grid gap-4 border-t border-slate-200 pt-7 sm:grid-cols-2"><div><p className="text-xs font-bold uppercase tracking-wider text-slate-500">Monthly payment</p><p className="mt-2 font-serif text-4xl">{money(result.monthly)}</p></div><div><p className="text-xs font-bold uppercase tracking-wider text-slate-500">Total amount paid</p><p className="mt-2 font-serif text-4xl">{money(result.total)}</p></div></div><p className="mt-5 text-xs text-slate-500">Based on a {money(result.principal)} loan. Estimates exclude taxes, insurance and fees.</p></div></AccessGate></div></section>};export default Calculator
